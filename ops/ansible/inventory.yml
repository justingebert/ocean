all:
  children:
    app:
      hosts:
        lima-ocean:
          ansible_ssh_common_args: "-F {{ lookup('env', 'HOME') }}/.lima/ocean/ssh.config"
          ansible_python_interpreter: /usr/bin/python3
