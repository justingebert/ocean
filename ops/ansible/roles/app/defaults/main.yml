env_src: "{{ repo_root }}/ops/multi-vm/env"
compose_src: "{{ repo_root }}/ops/multi-vm/app/docker-compose.yml"

force_build: true # Set -e force_build=false to skip rebuilds when images already exist.
