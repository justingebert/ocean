# /etc/ocean/mongodb_cluster.env (mongo-vm)
# Managed Mongo root credentials. Must match MONGODB_CLUSTER_* in backend.env on app-vm.

MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=change-me
