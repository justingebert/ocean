name: ocean-pg

services:
  pg_cluster:
    image: postgres:12.20
    restart: unless-stopped
    env_file: /etc/ocean/pg_cluster.env
    ports:
      - "5432:5432"
    volumes:
      - /srv/ocean/pg_cluster/data:/var/lib/postgresql/data
